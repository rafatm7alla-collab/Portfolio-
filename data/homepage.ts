/**
 * Which project gets the horizontal "closer look" gallery on the homepage.
 *
 * One project only. The device works because it is the exception — give it to
 * every project and it becomes the layout rather than a moment.
 *
 * Change the slug to move it. Set to null to remove the section entirely.
 */
export const closerLook: { slug: string | null; count: number } = {
  slug: 'toyota-crown',
  count: 6,
}
